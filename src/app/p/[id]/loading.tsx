/**
 * Soft-nav product opens InstantProductModalHost immediately from card data.
 * A skeleton here only appears on rare hard navigations and feels like a
 * blank product card — prefer empty so the instant shell can paint instead.
 */
export default function ProductLoading() {
  return null;
}
