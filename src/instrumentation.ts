/**
 * Server instrumentation hook (Next.js).
 * Browser RUM is initialized in `src/components/DatadogRum.tsx`.
 */
export function register() {
  // Intentionally empty — keep server register() free of browser-only SDKs.
}
